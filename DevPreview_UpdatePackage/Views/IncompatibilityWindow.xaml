<Window x:Class="Eternal.Views.IncompatibilityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        Title="System Incompatibility" Height="400" Width="550"
        WindowStyle="None" AllowsTransparency="True"
        WindowStartupLocation="CenterScreen" Background="Transparent">
    <Border Background="{DynamicResource BackgroundBrush}" CornerRadius="15" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1">
        <Grid Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,30">
                <fa:ImageAwesome Icon="ExclamationTriangle" Width="48" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,20"/>
                <TextBlock Text="INCOMPATIBLE OS DETECTED" FontSize="20" FontWeight="Black" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Body -->
            <StackPanel Grid.Row="1" HorizontalAlignment="Center">
                <TextBlock Text="Eternal System Intelligence requires Windows 10 or later." 
                           Foreground="{DynamicResource TextBrush}" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="Your current operating system is outdated and does not support the advanced diagnostic and recovery engines required for this suite." 
                           Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" TextWrapping="Wrap" TextAlignment="Center" LineHeight="18"/>
                
                <Border Background="{DynamicResource CardBrush}" Margin="0,25,0,0" Padding="15,10" CornerRadius="5">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="SUPPORTED:" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="10" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBlock Text=" Windows 10 (Build 19041+), Windows 11" Foreground="{DynamicResource AccentBrush}" FontSize="10" FontWeight="Black" VerticalAlignment="Center" Margin="5,0,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Actions -->
            <Button Grid.Row="2" Content="EXIT APPLICATION" Style="{StaticResource PrimaryButton}" Height="40" Width="200" Click="Exit_Click"/>
        </Grid>
    </Border>
</Window>


