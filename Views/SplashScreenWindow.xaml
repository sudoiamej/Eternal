<Window x:Class="Eternal.Views.SplashScreenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        Title="Eternal" Height="380" Width="550"
        WindowStyle="None" AllowsTransparency="True"
        WindowStartupLocation="CenterScreen" Background="Transparent" Topmost="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Opacity="0">
    
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.8">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <DoubleAnimation Storyboard.TargetName="MainScale" Storyboard.TargetProperty="ScaleX" From="0.92" To="1.0" Duration="0:0:1.2">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <DoubleAnimation Storyboard.TargetName="MainScale" Storyboard.TargetProperty="ScaleY" From="0.92" To="1.0" Duration="0:0:1.2">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Border CornerRadius="20" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <Border.Background>
            <RadialGradientBrush Center="0.5,0.4" GradientOrigin="0.5,0.4" RadiusX="0.7" RadiusY="0.7">
                <GradientStop Color="#1A1A1A" Offset="1"/>
                <GradientStop Color="#252525" Offset="0"/>
            </RadialGradientBrush>
        </Border.Background>
        
        <Grid x:Name="MainGrid" RenderTransformOrigin="0.5,0.5">
            <Grid.RenderTransform>
                <ScaleTransform x:Name="MainScale" ScaleX="1" ScaleY="1"/>
            </Grid.RenderTransform>
            
            <!-- Branded Content -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,0,40">
                <Image Source="/Assets/logo.png" Width="90" Margin="0,0,0,25">
                    <Image.Effect>
                        <DropShadowEffect Color="{DynamicResource AccentColor}" BlurRadius="30" ShadowDepth="0" Opacity="0.6"/>
                    </Image.Effect>
                </Image>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="ETERNAL" FontSize="42" FontWeight="Black" Foreground="White">
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="0" Color="Black" Opacity="0.5"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    <Border Background="#9B59B6" CornerRadius="4" VerticalAlignment="Top" Margin="12,8,0,0" Padding="8,2" BorderBrush="#33FFFFFF" BorderThickness="1">
                        <TextBlock Text="DEV PREVIEW" FontSize="10" FontWeight="Black" Foreground="White"/>
                    </Border>
                </StackPanel>
                
                <TextBlock Text="ADVANCED SYSTEM INTELLIGENCE" FontSize="11" FontWeight="SemiBold" 
                           Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center" 
                           Margin="0,5,0,0" Opacity="0.8"/>
            </StackPanel>

            <!-- Loading System (HCI: Continuous Feedback) -->
            <StackPanel VerticalAlignment="Bottom" Margin="0,0,0,50" Width="300">
                <TextBlock x:Name="StatusText" Text="Initializing Secure Diagnostic Tunnels..." 
                           FontSize="10" FontWeight="Medium" Foreground="{DynamicResource TextSecondaryBrush}" 
                           HorizontalAlignment="Center" Margin="0,0,0,12"/>
                
                <Grid>
                    <ProgressBar Height="2" IsIndeterminate="True" Background="#1FFFFFFF" Foreground="{DynamicResource AccentBrush}" BorderThickness="0"/>
                    <!-- Subtle Glow for the bar -->
                    <Border Height="2" Background="{DynamicResource AccentBrush}" Opacity="0.3">
                        <Border.Effect>
                            <BlurEffect Radius="10"/>
                        </Border.Effect>
                    </Border>
                </Grid>
            </StackPanel>

            <!-- Footer -->
            <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,20" Opacity="0.4">
                <TextBlock Text="© 2026 ETERNAL ANALYTICS • VERSION 2.5.0-M3" FontSize="8" FontWeight="Bold" Foreground="{DynamicResource TextDisabledBrush}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>


