<Window x:Class="Eternal.Views.SplashScreenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        Title="Eternal" Height="350" Width="500"
        WindowStyle="None" AllowsTransparency="True"
        WindowStartupLocation="CenterScreen" Background="Transparent" Topmost="True">
    <Border Background="{DynamicResource BackgroundBrush}" CornerRadius="12" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <Grid>
            <StackPanel VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,39,0,0">
                <Image Source="/Assets/logo.png" Width="80" Margin="0,0,0,20">
                    <Image.Effect>
                        <DropShadowEffect Color="{DynamicResource AccentColor}" BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
                    </Image.Effect>
                </Image>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="ETERNAL" FontSize="36" FontWeight="Black" Foreground="{DynamicResource TextBrush}"/>
                    <Border Background="#9B59B6" CornerRadius="4" VerticalAlignment="Top" Margin="10,5,0,0" Padding="6,2">
                        <TextBlock Text="CREATOR" FontSize="12" FontWeight="Black" Foreground="#FFFFFF"/>
                    </Border>
                </StackPanel>
                <TextBlock Text="SYSTEM INTELLIGENCE" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Loading Progress -->
            <StackPanel VerticalAlignment="Bottom" Margin="0,0,0,40">
                <fa:ImageAwesome Icon="Spinner" Spin="True" Foreground="{DynamicResource AccentBrush}" Width="24" Margin="0,0,0,10"/>
                <TextBlock x:Name="StatusText" Text="Initializing Intelligence Engine..." FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
            </StackPanel>

            <TextBlock Text="© 2026 ETERNAL ANALYTICS v2.5.0-M1" FontSize="8" Foreground="{DynamicResource TextDisabledBrush}" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,15"/>
        </Grid>
    </Border>
</Window>
