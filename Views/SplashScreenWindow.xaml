<Window x:Class="Eternal.Views.SplashScreenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        Title="Eternal" Height="360" Width="280"
        WindowStyle="None" AllowsTransparency="True"
        WindowStartupLocation="CenterScreen" Background="Transparent" Topmost="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Opacity="1"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Border CornerRadius="12" Background="#000000" BorderBrush="#1D1D22" BorderThickness="1" ClipToBounds="True">
        <Grid x:Name="MainGrid" Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Center Logo & Branding -->
            <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Image Source="/Assets/logo.png" Width="72" Height="72" Margin="0,0,0,25" HorizontalAlignment="Center"/>
                <TextBlock Text="ETERNAL" FontSize="26" FontWeight="Black" Foreground="#FFFFFF" HorizontalAlignment="Center"/>
                <TextBlock Text="DIAGNOSTICS WORKSTATION" FontSize="8" FontWeight="Bold" Foreground="#A2A2A9" HorizontalAlignment="Center" Margin="0,8,0,0"/>
            </StackPanel>

            <!-- Loader Indicators -->
            <StackPanel Grid.Row="1" Margin="0,0,0,10">
                <ProgressBar Height="2" Background="#111115" Foreground="#3A86FF" BorderThickness="0" IsIndeterminate="True" Margin="0,0,0,10"/>
                <TextBlock Text="INITIALIZING SECURE ENGINE..." FontSize="8" FontWeight="Bold" Foreground="#484850" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>