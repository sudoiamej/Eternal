<Window x:Class="Eternal.Views.Helpers.FirstRunSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        Title="Eternal System Initializer" Height="500" Width="540"
        WindowStyle="None" AllowsTransparency="True"
        WindowStartupLocation="CenterScreen" Background="Transparent"
        Topmost="True" SnapsToDevicePixels="True" UseLayoutRounding="True">
    
    <Border Background="#060609" CornerRadius="24" BorderBrush="#242436" BorderThickness="1.5" Opacity="0" RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <TransformGroup>
                <ScaleTransform x:Name="SetupScale" ScaleX="0.94" ScaleY="0.94"/>
                <TranslateTransform x:Name="SetupTranslate" Y="15"/>
            </TransformGroup>
        </Border.RenderTransform>

        <Border.Triggers>
            <EventTrigger RoutedEvent="Border.Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.45">
                            <DoubleAnimation.EasingFunction><QuadraticEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)" From="0.94" To="1" Duration="0:0:0.5">
                            <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)" From="0.94" To="1" Duration="0:0:0.5">
                            <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(TranslateTransform.Y)" From="15" To="0" Duration="0:0:0.5">
                            <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>

        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="35" ShadowDepth="4" Opacity="0.85"/>
        </Border.Effect>
        <Grid Margin="32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="Auto"/> <!-- Progress Bar -->
                <RowDefinition Height="*"/>    <!-- Live Log / Summary -->
                <RowDefinition Height="Auto"/> <!-- Action Button -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
                <Image Source="/Assets/logo.png" Height="56" HorizontalAlignment="Center" Margin="0,0,0,12" RenderOptions.BitmapScalingMode="HighQuality" SnapsToDevicePixels="True"/>
                <TextBlock Text="ETERNAL SYSTEM INITIALIZER" FontSize="16" FontWeight="Black" Foreground="#FFFFFF" HorizontalAlignment="Center"/>
                <Border Background="#1A0078D4" CornerRadius="4" Padding="8,2" Margin="0,6,0,0" HorizontalAlignment="Center">
                    <TextBlock Text="FIRST-TIME HARDWARE &amp; SUBSYSTEM BASELINE SCAN" Foreground="{DynamicResource AccentBrush}" FontSize="9" FontWeight="Bold" HorizontalAlignment="Center"/>
                </Border>
            </StackPanel>

            <!-- Progress Bar Section -->
            <StackPanel Grid.Row="1" Margin="0,0,0,18">
                <Grid Margin="0,0,0,8">
                    <TextBlock x:Name="StatusText" Text="Initializing system scan..." FontSize="11" FontWeight="Bold" Foreground="#D0D0DC" HorizontalAlignment="Left"/>
                    <TextBlock x:Name="PercentText" Text="0%" FontSize="11" FontWeight="Black" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Right"/>
                </Grid>
                <Border Height="8" Background="#12121C" CornerRadius="4" BorderBrush="#222232" BorderThickness="1">
                    <ProgressBar x:Name="ScanProgressBar" Minimum="0" Maximum="100" Value="0" Background="Transparent" Foreground="{DynamicResource AccentBrush}" BorderThickness="0"/>
                </Border>
            </StackPanel>

            <!-- Live Audit Logs & Summary Card Host -->
            <Grid Grid.Row="2" Margin="0,0,0,20">
                <!-- Log Terminal Box -->
                <Border x:Name="LogContainer" Background="#0C0C14" BorderBrush="#1F1F2E" BorderThickness="1" CornerRadius="12" Padding="14,10">
                    <ScrollViewer x:Name="LogScrollViewer" VerticalScrollBarVisibility="Auto">
                        <TextBlock x:Name="LogTerminalText" FontFamily="Consolas" FontSize="11" Foreground="#A0A0B0" TextWrapping="Wrap" LineHeight="18"/>
                    </ScrollViewer>
                </Border>

                <!-- Hardware Summary Card & Completion Prompt (Shown when complete) -->
                <Border x:Name="SummaryCard" Background="#0C0C14" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1" CornerRadius="12" Padding="18" Visibility="Collapsed">
                    <StackPanel>
                        <Border Background="#154CAF50" BorderBrush="#4CAF50" BorderThickness="1" CornerRadius="6" Padding="10,6" Margin="0,0,0,14">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <fa:ImageAwesome Icon="CheckCircle" Width="14" Foreground="#4CAF50" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="CALIBRATION &amp; INITIAL SCAN COMPLETE" FontSize="11" FontWeight="Black" Foreground="#4CAF50" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        
                        <Grid Margin="0,0,0,6">
                            <TextBlock Text="PROCESSOR:" FontSize="10" Foreground="#888896" FontWeight="Bold"/>
                            <TextBlock x:Name="CpuText" Text="Detecting..." FontSize="10" Foreground="#FFFFFF" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid Margin="0,0,0,6">
                            <TextBlock Text="MEMORY (RAM):" FontSize="10" Foreground="#888896" FontWeight="Bold"/>
                            <TextBlock x:Name="RamText" Text="Detecting..." FontSize="10" Foreground="#FFFFFF" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid Margin="0,0,0,6">
                            <TextBlock Text="STORAGE DISK:" FontSize="10" Foreground="#888896" FontWeight="Bold"/>
                            <TextBlock x:Name="DiskText" Text="Detecting..." FontSize="10" Foreground="#FFFFFF" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid>
                            <TextBlock Text="OPERATING SYSTEM:" FontSize="10" Foreground="#888896" FontWeight="Bold"/>
                            <TextBlock x:Name="OsText" Text="Detecting..." FontSize="10" Foreground="#FFFFFF" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Bottom Action Button -->
            <Grid Grid.Row="3">
                <Button x:Name="ContinueButton" Content="INITIALIZING..." Height="40" Width="240" Click="Continue_Click" IsEnabled="False" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="#1A1A28" BorderBrush="#2C2C40" BorderThickness="1" CornerRadius="10">
                                <TextBlock x:Name="btnText" Text="{TemplateBinding Content}" FontSize="11" FontWeight="Black" Foreground="#666678" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                    <Setter TargetName="btnText" Property="Foreground" Value="White"/>
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsEnabled" Value="True"/>
                                        <Condition Property="IsMouseOver" Value="True"/>
                                    </MultiTrigger.Conditions>
                                    <Setter TargetName="border" Property="Background" Value="#1F8AD2"/>
                                </MultiTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>
