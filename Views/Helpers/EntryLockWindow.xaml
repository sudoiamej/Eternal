<Window x:Class="Eternal.Views.Helpers.EntryLockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        Title="System Locked" Height="440" Width="480"
        WindowStyle="None" AllowsTransparency="True"
        WindowStartupLocation="CenterScreen" Background="Transparent"
        Topmost="True">

    <!-- Pure AMOLED Lock Screen Card -->
    <Border x:Name="RootBorder" Background="#060609" CornerRadius="24" BorderBrush="#242436" BorderThickness="1.5" Opacity="0" RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <TransformGroup>
                <ScaleTransform x:Name="LockScale" ScaleX="0.9" ScaleY="0.9"/>
                <TranslateTransform x:Name="LockTranslate" Y="20"/>
            </TransformGroup>
        </Border.RenderTransform>

        <Border.Triggers>
            <EventTrigger RoutedEvent="Border.Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4">
                            <DoubleAnimation.EasingFunction><QuadraticEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)" From="0.9" To="1" Duration="0:0:0.45">
                            <DoubleAnimation.EasingFunction><BackEase EasingMode="EaseOut" Amplitude="0.3"/></DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)" From="0.9" To="1" Duration="0:0:0.45">
                            <DoubleAnimation.EasingFunction><BackEase EasingMode="EaseOut" Amplitude="0.3"/></DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(TranslateTransform.Y)" From="20" To="0" Duration="0:0:0.45">
                            <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>

        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="35" ShadowDepth="4" Opacity="0.85"/>
        </Border.Effect>

        <Grid Margin="32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Branded Header -->
                <RowDefinition Height="*"/>    <!-- Windows Hello Content -->
                <RowDefinition Height="Auto"/> <!-- Bottom Actions -->
            </Grid.RowDefinitions>

            <!-- Branded Header -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,5,0,20">
                <Image Source="/Assets/logo.png" Height="48" HorizontalAlignment="Center" Margin="0,0,0,10" RenderOptions.BitmapScalingMode="HighQuality" SnapsToDevicePixels="True"/>
                <TextBlock Text="ETERNAL WORKSTATION LOCK" FontSize="15" FontWeight="Black" Foreground="#FFFFFF" HorizontalAlignment="Center"/>
                <Border Background="#1A0078D4" CornerRadius="4" Padding="8,2" Margin="0,6,0,0" HorizontalAlignment="Center">
                    <TextBlock Text="WINDOWS HELLO SECURITY VERIFICATION" Foreground="{DynamicResource AccentBrush}" FontSize="9" FontWeight="Bold" HorizontalAlignment="Center"/>
                </Border>
            </StackPanel>

            <!-- Windows Hello Interactive Card -->
            <Border Grid.Row="1" Background="#0C0C14" BorderBrush="#1F1F2E" BorderThickness="1" CornerRadius="16" Padding="24" VerticalAlignment="Center">
                <StackPanel HorizontalAlignment="Center">
                    <Border Width="64" Height="64" CornerRadius="32" Background="#1A3A86FF" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1" HorizontalAlignment="Center" Margin="0,0,0,16">
                        <fa:ImageAwesome Icon="Shield" Width="26" Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>

                    <TextBlock Text="WINDOWS HELLO SYSTEM VERIFICATION" FontSize="12" FontWeight="Black" Foreground="#FFFFFF" HorizontalAlignment="Center" Margin="0,0,0,6"/>
                    <TextBlock x:Name="StatusSubtext" Text="Verify your identity with Windows Hello (System PIN, Fingerprint, or Face) to unlock." 
                               FontSize="11" Foreground="#A0A0B0" TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,20"/>

                    <Button x:Name="WindowsHelloButton" Click="WindowsHello_Click" Height="44" Width="260" Cursor="Hand" Style="{StaticResource PrimaryButton}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <fa:ImageAwesome Icon="Lock" Width="13" Foreground="White" Margin="0,0,10,0"/>
                            <TextBlock Text="VERIFY WITH WINDOWS HELLO" FontSize="11" FontWeight="Black" Foreground="White"/>
                        </StackPanel>
                    </Button>

                    <TextBlock x:Name="ErrorText" Foreground="{DynamicResource CriticalBrush}" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,12,0,0" Visibility="Collapsed"/>
                </StackPanel>
            </Border>

            <!-- Bottom Actions -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button Content="EXIT APPLICATION" Click="Exit_Click" Style="{StaticResource SecondaryButton}" Height="34" Padding="20,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
