<Window x:Class="Eternal.Views.Helpers.NetUserInspectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        mc:Ignorable="d"
        Title="Net User Telemetry Inspector" Height="620" Width="680"
        WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border x:Name="RootBorder" Background="#060609" BorderBrush="#1C1C28" BorderThickness="1.5" CornerRadius="16" Padding="28" Opacity="0" RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <TransformGroup>
                <ScaleTransform x:Name="WindowScale" ScaleX="0.92" ScaleY="0.92"/>
                <TranslateTransform x:Name="WindowTranslate" Y="15"/>
            </TransformGroup>
        </Border.RenderTransform>
        <Border.Triggers>
            <EventTrigger RoutedEvent="Border.Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="RootBorder" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.25">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetName="WindowScale" Storyboard.TargetProperty="ScaleX" To="1" Duration="0:0:0.25">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetName="WindowScale" Storyboard.TargetProperty="ScaleY" To="1" Duration="0:0:0.25">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetName="WindowTranslate" Storyboard.TargetProperty="Y" To="0" Duration="0:0:0.25">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header Title & Close -->
                <RowDefinition Height="*"/>    <!-- SAM Account Metrics & Groups -->
                <RowDefinition Height="Auto"/> <!-- Raw Output Accordion / Actions -->
            </Grid.RowDefinitions>

            <!-- Row 0: Header -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <fa:ImageAwesome Icon="Shield" Width="14" Foreground="{DynamicResource AccentBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="NET USER SAM TELEMETRY" FontSize="9" FontWeight="Black" Foreground="{DynamicResource AccentBrush}"/>
                    </StackPanel>
                    <TextBlock x:Name="HeaderUsernameText" Text="ACCOUNT SECURITY INSPECTOR" FontSize="20" FontWeight="Black" Foreground="White"/>
                </StackPanel>

                <!-- Close Button -->
                <Button Grid.Column="1" Click="CloseButton_Click" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" Background="Transparent" BorderThickness="0" Cursor="Hand" VerticalAlignment="Top">
                    <fa:ImageAwesome Icon="Times" Width="14" Foreground="#888896"/>
                </Button>
            </Grid>

            <!-- Row 1: Content Body -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Account Overview Header Card -->
                    <Border Background="#0D0D14" BorderBrush="#1F1F2E" BorderThickness="1" CornerRadius="12" Padding="18" Margin="0,0,0,16">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Background="#161622" CornerRadius="10" Width="50" Height="50" Margin="0,0,16,0">
                                <fa:ImageAwesome Icon="User" Width="22" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,3">
                                    <TextBlock x:Name="UsernameTitleText" Text="Username" FontSize="16" FontWeight="Black" Foreground="White" Margin="0,0,10,0"/>
                                    <Border x:Name="AccountStatusBorder" Background="#102014" BorderBrush="#22C55E" BorderThickness="1" CornerRadius="4" Padding="6,2" VerticalAlignment="Center">
                                        <TextBlock x:Name="AccountStatusText" Text="ACTIVE ACCOUNT" FontSize="8" FontWeight="Black" Foreground="#22C55E"/>
                                    </Border>
                                </StackPanel>
                                <TextBlock x:Name="FullNameText" Text="Full Name: N/A" FontSize="11" Foreground="#888896"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                <TextBlock Text="SECURITY AUDIT" FontSize="8" FontWeight="Black" Foreground="#555562" HorizontalAlignment="Right"/>
                                <TextBlock x:Name="LogonHoursText" Text="Logon Hours: All" FontSize="10" FontWeight="Bold" Foreground="#CCCCCC" HorizontalAlignment="Right" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Security Metrics Grid -->
                    <TextBlock Text="ACCOUNT SECURITY METRICS" FontSize="9" FontWeight="Bold" Foreground="#555562" Margin="4,0,0,8"/>
                    <UniformGrid Columns="2" Margin="0,0,0,16">
                        <Border Background="#0A0A10" BorderBrush="#181824" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,0,4,8">
                            <StackPanel>
                                <TextBlock Text="PASSWORD LAST SET" FontSize="8" FontWeight="Bold" Foreground="#888896"/>
                                <TextBlock x:Name="PasswordLastSetText" Text="Loading..." FontSize="11" FontWeight="Bold" Foreground="White" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Background="#0A0A10" BorderBrush="#181824" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="4,0,0,8">
                            <StackPanel>
                                <TextBlock Text="PASSWORD EXPIRES" FontSize="8" FontWeight="Bold" Foreground="#888896"/>
                                <TextBlock x:Name="PasswordExpiresText" Text="Loading..." FontSize="11" FontWeight="Bold" Foreground="White" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Background="#0A0A10" BorderBrush="#181824" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,0,4,0">
                            <StackPanel>
                                <TextBlock Text="LAST LOGON TIMESTAMP" FontSize="8" FontWeight="Bold" Foreground="#888896"/>
                                <TextBlock x:Name="LastLogonText" Text="Loading..." FontSize="11" FontWeight="Bold" Foreground="White" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Background="#0A0A10" BorderBrush="#181824" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="4,0,0,0">
                            <StackPanel>
                                <TextBlock Text="PASSWORD REQUIRED" FontSize="8" FontWeight="Bold" Foreground="#888896"/>
                                <TextBlock x:Name="PasswordRequiredText" Text="Loading..." FontSize="11" FontWeight="Bold" Foreground="White" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </UniformGrid>

                    <!-- Group Memberships -->
                    <TextBlock Text="LOCAL GROUP MEMBERSHIPS" FontSize="9" FontWeight="Bold" Foreground="#555562" Margin="4,0,0,8"/>
                    <Border Background="#0A0A10" BorderBrush="#181824" BorderThickness="1" CornerRadius="8" Padding="14,10" Margin="0,0,0,16">
                        <ItemsControl x:Name="GroupsItemsControl">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#161626" BorderBrush="#0078D4" BorderThickness="1" CornerRadius="6" Padding="10,4" Margin="0,0,8,6">
                                        <StackPanel Orientation="Horizontal">
                                            <fa:ImageAwesome Icon="Users" Width="10" Foreground="{DynamicResource AccentBrush}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding}" FontSize="11" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>

                    <!-- Expandable Raw CLI Terminal Output -->
                    <Expander Header="RAW CLI TERMINAL OUTPUT (net user)" Foreground="#888896" FontSize="10" FontWeight="Bold" IsExpanded="False" Margin="0,0,0,10">
                        <Border Background="#040407" BorderBrush="#141420" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,8,0,0">
                            <TextBox x:Name="RawOutputTextBox" Background="Transparent" BorderThickness="0" Foreground="#00FF66" FontFamily="Consolas" FontSize="11" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" MaxHeight="180"/>
                        </Border>
                    </Expander>
                </StackPanel>
            </ScrollViewer>

            <!-- Row 2: Footer Controls -->
            <Grid Grid.Row="2" Margin="0,15,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Click="CopyRawOutput_Click" Style="{StaticResource StandardButton}" Height="34" HorizontalAlignment="Left" Padding="12,0" Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <fa:ImageAwesome Icon="Copy" Width="11" Foreground="#888896" Margin="0,0,6,0"/>
                        <TextBlock Text="Copy Raw Telemetry" FontSize="10" FontWeight="Bold" Foreground="White"/>
                    </StackPanel>
                </Button>

                <Button Grid.Column="1" Click="CloseButton_Click" Style="{StaticResource PrimaryButton}" Height="34" Width="90" Cursor="Hand">
                    <TextBlock Text="CLOSE" FontWeight="Black" FontSize="11" Foreground="Black"/>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>
