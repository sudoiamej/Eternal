<Window x:Class="Eternal.Views.Helpers.BsodSimulatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        mc:Ignorable="d"
        Title="BSOD Crash Dump Diagnostic Simulator" Height="620" Width="740"
        WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border Background="#060609" BorderBrush="#EF4444" BorderThickness="1.5" CornerRadius="16" Padding="28">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <fa:ImageAwesome Icon="ExclamationTriangle" Width="14" Foreground="#EF4444" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="FORENSIC CRASH DRILL SIMULATOR" FontSize="9" FontWeight="Black" Foreground="#EF4444"/>
                    </StackPanel>
                    <TextBlock Text="BSOD MINIDUMP ANALYZER DRILL" FontSize="20" FontWeight="Black" Foreground="White"/>
                </StackPanel>

                <Button Grid.Column="1" Click="CloseButton_Click" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" Background="Transparent" BorderThickness="0" Cursor="Hand">
                    <fa:ImageAwesome Icon="Times" Width="14" Foreground="#888896"/>
                </Button>
            </Grid>

            <!-- Crash Analysis Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- BSOD Blue Screen Simulation Banner -->
                    <Border Background="#0A2540" BorderBrush="#0078D4" BorderThickness="1" CornerRadius="12" Padding="20" Margin="0,0,0,16">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text=":(" FontSize="42" FontWeight="Bold" Foreground="White" Margin="0,0,20,0" VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="SYSTEM CRASH DUMP ANALYSIS SIMULATION" FontSize="12" FontWeight="Black" Foreground="#60A5FA"/>
                                <TextBlock Text="BugCheck 0x000000D1: DRIVER_IRQL_NOT_LESS_OR_EQUAL" FontSize="13" FontWeight="Bold" Foreground="White" Margin="0,4,0,0"/>
                                <TextBlock Text="Failing Driver: nvlddmkm.sys (NVIDIA Display Kernel Driver)" FontSize="11" Foreground="#93C5FD" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Crash Dump Metrics Grid -->
                    <TextBlock Text="PARSED MINIDUMP METRICS" FontSize="9" FontWeight="Bold" Foreground="#555562" Margin="4,0,0,8"/>
                    <UniformGrid Columns="2" Margin="0,0,0,16">
                        <Border Background="#0A0A10" BorderBrush="#181824" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,0,4,8">
                            <StackPanel>
                                <TextBlock Text="STOP CODE" FontSize="8" FontWeight="Bold" Foreground="#888896"/>
                                <TextBlock Text="0x000000D1" FontSize="11" FontWeight="Bold" Foreground="#EF4444" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Background="#0A0A10" BorderBrush="#181824" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="4,0,0,8">
                            <StackPanel>
                                <TextBlock Text="FAULTING MODULE" FontSize="8" FontWeight="Bold" Foreground="#888896"/>
                                <TextBlock Text="nvlddmkm.sys (Offset 0x1A42F0)" FontSize="11" FontWeight="Bold" Foreground="White" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Background="#0A0A10" BorderBrush="#181824" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,0,4,0">
                            <StackPanel>
                                <TextBlock Text="PROCESS IN CONTEXT" FontSize="8" FontWeight="Bold" Foreground="#888896"/>
                                <TextBlock Text="dwm.exe (Desktop Window Manager)" FontSize="11" FontWeight="Bold" Foreground="White" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Background="#0A0A10" BorderBrush="#181824" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="4,0,0,0">
                            <StackPanel>
                                <TextBlock Text="RECOMMENDED REMEDIATION" FontSize="8" FontWeight="Bold" Foreground="#888896"/>
                                <TextBlock Text="Reinstall GPU Driver via DDU" FontSize="11" FontWeight="Bold" Foreground="#22C55E" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </UniformGrid>

                    <!-- Simulated Stack Trace Log -->
                    <TextBlock Text="SIMULATED WINDBG CALL STACK TELEMETRY" FontSize="9" FontWeight="Bold" Foreground="#555562" Margin="4,0,0,8"/>
                    <Border Background="#040407" BorderBrush="#141420" BorderThickness="1" CornerRadius="8" Padding="14">
                        <TextBlock FontFamily="Consolas" FontSize="11" Foreground="#00FF66" TextWrapping="Wrap">
fffff801`4521a000 nvlddmkm!nvQueryKernelPointer + 0x4f2&#x0a;
fffff801`4521a150 dnkern!KmDispatchMemoryAccess + 0x1c8&#x0a;
fffff801`4521a200 ntoskrnl!KiPageFault + 0x360&#x0a;
STATUS: Isolated failing third-party GPU kernel component cleanly.
                        </TextBlock>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer Controls -->
            <Grid Grid.Row="2" Margin="0,15,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="SAFE DIAGNOSTIC DRILL MODE — NO SYSTEM ALTERATION" FontSize="10" FontWeight="Bold" Foreground="#888896" VerticalAlignment="Center"/>

                <Button Grid.Column="1" Click="CloseButton_Click" Style="{StaticResource PrimaryButton}" Height="34" Width="90" Cursor="Hand">
                    <TextBlock Text="CLOSE" FontWeight="Black" FontSize="11" Foreground="Black"/>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>
