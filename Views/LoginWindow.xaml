<Window x:Class="Eternal.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        mc:Ignorable="d"
        Title="Eternal System Access" Height="580" Width="440"
        WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border Background="#050508" BorderBrush="#1C1C28" BorderThickness="1" CornerRadius="20" Padding="35">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header branding -->
                <RowDefinition Height="*"/>    <!-- Input fields & Auth form -->
                <RowDefinition Height="Auto"/> <!-- Footer status -->
            </Grid.RowDefinitions>

            <!-- Close Button -->
            <Button VerticalAlignment="Top" HorizontalAlignment="Right" Style="{StaticResource IconButton}" Click="Close_Click" ToolTip="Exit Application">
                <fa:ImageAwesome Icon="Close" Width="12" Foreground="#888896"/>
            </Button>

            <!-- Header Branding -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,15,0,30">
                <Image Source="/Assets/logo.png" Width="72" Height="72" HorizontalAlignment="Center" Margin="0,0,0,18">
                    <Image.Effect>
                        <DropShadowEffect Color="#0078D4" BlurRadius="25" ShadowDepth="0" Opacity="0.4"/>
                    </Image.Effect>
                </Image>
                <TextBlock Text="ETERNAL" FontSize="28" FontWeight="Black" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="STRICT WORKSTATION IDENTITY" FontSize="9" FontWeight="Black" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" Margin="0,4,0,0" Opacity="0.9"/>
            </StackPanel>

            <!-- Auth Input Form -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <!-- User Profile Field -->
                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="WINDOWS USER PROFILE" Style="{StaticResource Caption}" FontSize="9" Foreground="#888896" Margin="0,0,0,8"/>
                    <Border Background="#0C0C14" BorderBrush="#1F1F2E" BorderThickness="1" CornerRadius="8" Padding="14,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <fa:ImageAwesome Grid.Column="0" Icon="User" Width="14" Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <TextBlock Grid.Column="1" x:Name="UsernameTextBlock" Text="User" FontSize="13" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- Password / PIN Input Field -->
                <StackPanel Margin="0,0,0,25">
                    <TextBlock Text="WINDOWS ACCOUNT PASSWORD / PIN" Style="{StaticResource Caption}" FontSize="9" Foreground="#888896" Margin="0,0,0,8"/>
                    <Border Background="#0C0C14" BorderBrush="#1F1F2E" BorderThickness="1" CornerRadius="8" Padding="14,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <fa:ImageAwesome Grid.Column="0" Icon="Lock" Width="14" Foreground="#888896" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <PasswordBox Grid.Column="1" x:Name="PasswordInputBox" Background="Transparent" BorderThickness="0" Foreground="White" FontSize="14" PasswordChanged="PasswordInputBox_PasswordChanged" KeyDown="PasswordInputBox_KeyDown"/>
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- Enter Button (Primary Auth) -->
                <Button x:Name="EnterButton" Click="EnterButton_Click" Style="{StaticResource PrimaryButton}" Height="46" Cursor="Hand" Margin="0,0,0,15">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <fa:ImageAwesome Icon="Key" Width="14" Foreground="Black" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="ENTER WORKSTATION" FontWeight="Black" FontSize="12" Foreground="Black" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- Windows Hello Trigger Button -->
                <Button Click="WindowsHelloButton_Click" Style="{StaticResource StandardButton}" Height="40" Cursor="Hand">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <fa:ImageAwesome Icon="Windows" Width="12" Foreground="{DynamicResource AccentBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Use Windows Hello PIN / Biometrics" FontSize="11" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <!-- Footer Error/Status Message & Emergency Recovery -->
            <StackPanel Grid.Row="2" HorizontalAlignment="Center">
                <Button Click="EmergencyRecoveryButton_Click" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" Background="Transparent" BorderThickness="0" HorizontalAlignment="Center" Margin="0,0,0,6" Cursor="Hand" ToolTip="Use active logged-in Windows desktop session token">
                    <TextBlock Text="Emergency Desktop Session Access" FontSize="9" Foreground="#666675" TextDecorations="Underline"/>
                </Button>
                <TextBlock x:Name="StatusTextBlock" Text="Identity verification required to proceed." FontSize="10" Foreground="#888896" FontWeight="SemiBold" HorizontalAlignment="Center" TextAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
