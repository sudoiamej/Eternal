<UserControl x:Class="Eternal.Views.Modules.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" d:DesignHeight="720" d:DesignWidth="1000">
    <Border Padding="40">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="*"/>    <!-- Content -->
                <RowDefinition Height="Auto"/> <!-- Status -->
            </Grid.RowDefinitions>

            <!-- 1. Header -->
            <Grid Grid.Row="0" Margin="0,0,0,30">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <TextBlock Text="ETERNAL" Style="{StaticResource Caption}" Foreground="{DynamicResource AccentBrush}"/>
                        <TextBlock Text=" › " Style="{StaticResource Caption}"/>
                        <TextBlock Text="USER MANAGEMENT" Style="{StaticResource Caption}"/>
                    </StackPanel>
                    <TextBlock Text="Local Users &amp; Groups" Style="{StaticResource H1}" Margin="0"/>
                </StackPanel>
                
                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Bottom" Orientation="Horizontal">
                    <Button Command="{Binding LoadDataCommand}" Style="{StaticResource IconButton}" ToolTip="Refresh Data">
                        <fa:ImageAwesome Icon="Refresh" Width="14" Foreground="{DynamicResource TextSecondaryBrush}" Spin="{Binding IsBusy}"><fa:ImageAwesome.Effect><DropShadowEffect Color="{DynamicResource AccentColor}" BlurRadius="15" ShadowDepth="0" Opacity="0.3"/></fa:ImageAwesome.Effect></fa:ImageAwesome>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- 2. Content -->
            <TabControl Grid.Row="1" Style="{StaticResource ModernTabControl}">
                <TabItem Header="USERS">
                    <Border Style="{StaticResource StandardCard}" Padding="0" Margin="0,20,0,0">
                        <DataGrid ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}" 
                                  AutoGenerateColumns="False" IsReadOnly="True" Background="Transparent"
                                  BorderThickness="0" RowBackground="Transparent" GridLinesVisibility="None"
                                  Foreground="{DynamicResource TextBrush}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="USERNAME" Binding="{Binding Name}" Width="150"/>
                                <DataGridTextColumn Header="FULL NAME" Binding="{Binding FullName}" Width="200"/>
                                <DataGridTextColumn Header="STATUS" Binding="{Binding IsEnabled, Converter={StaticResource BooleanToStatusConverter}}" Width="100"/>
                                <DataGridTextColumn Header="SID" Binding="{Binding Sid}" Width="*"/>
                                <DataGridTemplateColumn Header="ACTIONS" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Command="{Binding DataContext.ToggleUserStatusCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                        CommandParameter="{Binding}" Style="{StaticResource IconButton}" ToolTip="Toggle Status">
                                                    <fa:ImageAwesome Icon="PowerOff" Width="12" Foreground="{DynamicResource TextSecondaryBrush}"><fa:ImageAwesome.Effect><DropShadowEffect Color="{DynamicResource AccentColor}" BlurRadius="15" ShadowDepth="0" Opacity="0.3"/></fa:ImageAwesome.Effect></fa:ImageAwesome>
                                                </Button>
                                                <Button Command="{Binding DataContext.DeleteUserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                        CommandParameter="{Binding}" Style="{StaticResource IconButton}" ToolTip="Delete User" Margin="10,0,0,0">
                                                    <fa:ImageAwesome Icon="Trash" Width="12" Foreground="{DynamicResource CriticalBrush}"><fa:ImageAwesome.Effect><DropShadowEffect Color="{DynamicResource AccentColor}" BlurRadius="15" ShadowDepth="0" Opacity="0.3"/></fa:ImageAwesome.Effect></fa:ImageAwesome>
                                                </Button>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </TabItem>
                    
                <TabItem Header="GROUPS">
                    <Border Style="{StaticResource StandardCard}" Padding="0" Margin="0,20,0,0">
                        <DataGrid ItemsSource="{Binding Groups}" SelectedItem="{Binding SelectedGroup}" 
                                  AutoGenerateColumns="False" IsReadOnly="True" Background="Transparent"
                                  BorderThickness="0" RowBackground="Transparent" GridLinesVisibility="None"
                                  Foreground="{DynamicResource TextBrush}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="GROUP NAME" Binding="{Binding Name}" Width="200"/>
                                <DataGridTextColumn Header="DESCRIPTION" Binding="{Binding Description}" Width="*"/>
                                <DataGridTemplateColumn Header="ACTIONS" Width="80">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Command="{Binding DataContext.DeleteGroupCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                    CommandParameter="{Binding}" Style="{StaticResource IconButton}" ToolTip="Delete Group">
                                                <fa:ImageAwesome Icon="Trash" Width="12" Foreground="{DynamicResource CriticalBrush}"><fa:ImageAwesome.Effect><DropShadowEffect Color="{DynamicResource AccentColor}" BlurRadius="15" ShadowDepth="0" Opacity="0.3"/></fa:ImageAwesome.Effect></fa:ImageAwesome>
                                            </Button>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </TabItem>
            </TabControl>

            <!-- 3. Status Bar -->
            <Border Grid.Row="2" Style="{StaticResource StandardCard}" Padding="15,10" Margin="0,20,0,0">
                <Grid>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" FontSize="11"/>
                    <fa:ImageAwesome Icon="Spinner" Spin="True" Width="12" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Right" 
                                     Visibility="{Binding IsBusy, Converter={StaticResource VisibilityConverter}}"><fa:ImageAwesome.Effect><DropShadowEffect Color="{DynamicResource AccentColor}" BlurRadius="15" ShadowDepth="0" Opacity="0.3"/></fa:ImageAwesome.Effect></fa:ImageAwesome>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>




