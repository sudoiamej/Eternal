<UserControl x:Class="Eternal.Views.Modules.BiosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Storyboard x:Key="ModuleEntrance">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="25" To="0" Duration="0:0:0.6">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </UserControl.Resources>

    <UserControl.RenderTransform>
        <TranslateTransform Y="0"/>
    </UserControl.RenderTransform>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="UserControl.Loaded">
            <BeginStoryboard Storyboard="{StaticResource ModuleEntrance}"/>
        </EventTrigger>
    </UserControl.Triggers>

    <Border Padding="40">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Header -->
                    <RowDefinition Height="Auto"/> <!-- Firmware Foundation -->
                    <RowDefinition Height="*"/>    <!-- Boot Security -->
                </Grid.RowDefinitions>

                <!-- 1. Unified Header (HCI Rule 9: Consistency) -->
                <Grid Grid.Row="0" Style="{StaticResource ModuleHeaderStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="ETERNAL" Style="{StaticResource Caption}" Foreground="{DynamicResource AccentBrush}"/>
                            <TextBlock Text=" › " Style="{StaticResource Caption}"/>
                            <TextBlock Text="FIRMWARE &amp; BOOT" Style="{StaticResource Caption}"/>
                        </StackPanel>
                        <TextBlock Text="BIOS / UEFI Intelligence" Style="{StaticResource H1}" Margin="0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" VerticalAlignment="Bottom" Margin="0,0,0,5">
                        <Button Command="{Binding LoadCommand}" IsEnabled="{Binding IsBusy, Converter={StaticResource VisibilityConverter}, ConverterParameter=Inverse}" Style="{StaticResource IconButton}" ToolTip="Query Firmware Info">
                            <fa:ImageAwesome Icon="Refresh" Width="16" Foreground="{DynamicResource TextSecondaryBrush}"><fa:ImageAwesome.Effect><DropShadowEffect Color="Transparent" BlurRadius="0" ShadowDepth="0" Opacity="0"/></fa:ImageAwesome.Effect></fa:ImageAwesome>
                        </Button>
                    </StackPanel>
                </Grid>
                
                <!-- 2. Firmware Foundation Card (HCI Rule 6: Conceptual Model) -->
                <Border Grid.Row="1" Style="{StaticResource StandardCard}" Margin="0,0,0,25" Padding="25">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <fa:ImageAwesome Icon="InfoCircle" Width="32" Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Top" Margin="0,5,30,0"><fa:ImageAwesome.Effect><DropShadowEffect Color="Transparent" BlurRadius="0" ShadowDepth="0" Opacity="0"/></fa:ImageAwesome.Effect></fa:ImageAwesome>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="FIRMWARE IDENTITY" Style="{StaticResource Caption}" Margin="0,0,0,15"/>
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Manufacturer" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
                                    <TextBlock Text="{Binding Bios.Vendor}" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>
                                    
                                    <TextBlock Text="Firmware Version" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
                                    <TextBlock Text="{Binding Bios.Version}" FontSize="18" FontWeight="Bold" FontFamily="Consolas"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Release Date" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
                                    <TextBlock Text="{Binding Bios.ReleaseDate}" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>
                                    
                                    <TextBlock Text="System Architecture" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
                                    <TextBlock Text="x64 (64-bit)" FontSize="18" FontWeight="Bold"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 3. Boot Security Audit (HCI Rule 7 & 14: Status & Evaluation) -->
                <StackPanel Grid.Row="2">
                    <TextBlock Text="BOOT SECURITY AUDIT" Style="{StaticResource Caption}" Margin="0,0,0,15"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <Border Grid.Column="0" Style="{StaticResource StandardCard}" Margin="0,0,10,0" Padding="20">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="FIRMWARE MODE" Style="{StaticResource Caption}" Margin="0,0,0,10"/>
                                <StackPanel Orientation="Horizontal">
                                    <fa:ImageAwesome Icon="CheckCircle" Width="16" Foreground="{DynamicResource SuccessBrush}" VerticalAlignment="Center" Margin="0,0,10,0" Visibility="{Binding Uefi.IsUefi, Converter={StaticResource VisibilityConverter}}"><fa:ImageAwesome.Effect><DropShadowEffect Color="Transparent" BlurRadius="0" ShadowDepth="0" Opacity="0"/></fa:ImageAwesome.Effect></fa:ImageAwesome>
                                    <TextBlock Text="UEFI (Modern)" FontSize="20" FontWeight="Bold" Visibility="{Binding Uefi.IsUefi, Converter={StaticResource VisibilityConverter}}"/>
                                    
                                    <fa:ImageAwesome Icon="ExclamationTriangle" Width="16" Foreground="{DynamicResource WarningBrush}" VerticalAlignment="Center" Margin="0,0,10,0" Visibility="{Binding Uefi.IsUefi, Converter={StaticResource VisibilityConverter}, ConverterParameter=Inverse}"><fa:ImageAwesome.Effect><DropShadowEffect Color="Transparent" BlurRadius="0" ShadowDepth="0" Opacity="0"/></fa:ImageAwesome.Effect></fa:ImageAwesome>
                                    <TextBlock Text="Legacy (BIOS)" FontSize="20" FontWeight="Bold" Visibility="{Binding Uefi.IsUefi, Converter={StaticResource VisibilityConverter}, ConverterParameter=Inverse}"/>
                                </StackPanel>
                                <TextBlock Text="Ensures modern hardware interface features." FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="1" Style="{StaticResource StandardCard}" Margin="10,0,0,0" Padding="20">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="SECURE BOOT" Style="{StaticResource Caption}" Margin="0,0,0,10"/>
                                <StackPanel Orientation="Horizontal">
                                    <fa:ImageAwesome Icon="Shield" Width="16" Foreground="{DynamicResource SuccessBrush}" VerticalAlignment="Center" Margin="0,0,10,0" Visibility="{Binding Uefi.SecureBootEnabled, Converter={StaticResource VisibilityConverter}}"><fa:ImageAwesome.Effect><DropShadowEffect Color="Transparent" BlurRadius="0" ShadowDepth="0" Opacity="0"/></fa:ImageAwesome.Effect></fa:ImageAwesome>
                                    <TextBlock Text="ENABLED" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}" Visibility="{Binding Uefi.SecureBootEnabled, Converter={StaticResource VisibilityConverter}}"/>
                                    
                                    <fa:ImageAwesome Icon="Shield" Width="16" Foreground="{DynamicResource CriticalBrush}" VerticalAlignment="Center" Margin="0,0,10,0" Visibility="{Binding Uefi.SecureBootEnabled, Converter={StaticResource VisibilityConverter}, ConverterParameter=Inverse}"><fa:ImageAwesome.Effect><DropShadowEffect Color="Transparent" BlurRadius="0" ShadowDepth="0" Opacity="0"/></fa:ImageAwesome.Effect></fa:ImageAwesome>
                                    <TextBlock Text="DISABLED" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource CriticalBrush}" Visibility="{Binding Uefi.SecureBootEnabled, Converter={StaticResource VisibilityConverter}, ConverterParameter=Inverse}"/>
                                </StackPanel>
                                <TextBlock Text="Protects boot process from unauthorized code." FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Border Style="{StaticResource StandardCard}" Margin="0,20,0,0" Padding="25">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <fa:ImageAwesome Icon="Lock" Width="32" Foreground="{DynamicResource InfoBrush}" VerticalAlignment="Top" Margin="0,5,30,0"><fa:ImageAwesome.Effect><DropShadowEffect Color="Transparent" BlurRadius="0" ShadowDepth="0" Opacity="0"/></fa:ImageAwesome.Effect></fa:ImageAwesome>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="TPM (TRUSTED PLATFORM MODULE) DETAILS" Style="{StaticResource Caption}" Margin="0,0,0,15"/>
                                
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Status" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
                                        <TextBlock Text="{Binding Uefi.Tpm.Status}" FontSize="16" FontWeight="Bold" Margin="0,0,0,12"/>

                                        <TextBlock Text="Specification Version" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
                                        <TextBlock Text="{Binding Uefi.Tpm.Version}" FontSize="16" FontWeight="Bold"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="Manufacturer" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
                                        <TextBlock Text="{Binding Uefi.Tpm.Manufacturer}" FontSize="16" FontWeight="Bold" Margin="0,0,0,12"/>

                                        <TextBlock Text="Manufacturer ID" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
                                        <TextBlock Text="{Binding Uefi.Tpm.ManufacturerId}" FontSize="16" FontWeight="Bold" FontFamily="Consolas"/>
                                    </StackPanel>
                                </Grid>
                                
                                <Separator Background="{DynamicResource BorderBrush}" Margin="0,20,0,15"/>
                                <TextBlock Text="The TPM provides hardware-based, security-related functions for attestation and encryption." 
                                           FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- Loading Overlay (HCI Rule 7: Status) -->
                <Border Grid.RowSpan="3" Background="#CC0A0A0A" Visibility="{Binding IsBusy, Converter={StaticResource VisibilityConverter}}" Margin="-40">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <fa:ImageAwesome Icon="Spinner" Spin="True" Foreground="{DynamicResource AccentBrush}" Width="48" Margin="0,0,0,20"><fa:ImageAwesome.Effect><DropShadowEffect Color="Transparent" BlurRadius="0" ShadowDepth="0" Opacity="0"/></fa:ImageAwesome.Effect></fa:ImageAwesome>
                        <TextBlock Text="Auditing Firmware Foundation..." FontSize="18" Foreground="{DynamicResource TextBrush}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>
    </Border>
</UserControl>
