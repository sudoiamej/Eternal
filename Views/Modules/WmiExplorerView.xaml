<UserControl x:Class="Eternal.Views.Modules.WmiExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1000">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Preset Bar -->
            <RowDefinition Height="Auto"/> <!-- Query Input -->
            <RowDefinition Height="*"/>    <!-- Results Grid -->
            <RowDefinition Height="Auto"/> <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="ETERNAL SYSTEM WORKSPACE" Style="{StaticResource Caption}" Foreground="{DynamicResource AccentBrush}"/>
            <TextBlock Text="WMI Telemetry Explorer" Style="{StaticResource H1}" Margin="0,2,0,4"/>
            <TextBlock Text="Run raw WQL queries against local CIM/WMI repositories with bare-metal precision." Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11"/>
        </StackPanel>

        <!-- Presets Quick Bar -->
        <Border Grid.Row="1" Background="#14141C" BorderBrush="#252535" BorderThickness="1" CornerRadius="8" Padding="12,8" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,12,0">
                    <fa:ImageAwesome Icon="List" Width="11" Foreground="{DynamicResource AccentBrush}" Margin="0,0,6,0"/>
                    <TextBlock Text="PRESETS:" FontSize="10" FontWeight="Bold" Foreground="#9E9EA8" VerticalAlignment="Center"/>
                </StackPanel>
                <ComboBox Grid.Column="1" ItemsSource="{Binding Presets}" SelectedItem="{Binding Query}" 
                          Height="28" FontSize="11" VerticalContentAlignment="Center"
                          Background="#1C1C26" Foreground="#FFFFFF" BorderBrush="#333348">
                </ComboBox>
            </Grid>
        </Border>

        <!-- Query Input Area -->
        <Border Grid.Row="2" Style="{StaticResource StandardCard}" Margin="0,0,0,15" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource ModernTextBox}" 
                         Height="42" VerticalContentAlignment="Center" FontSize="13"
                         FontFamily="Consolas" Foreground="#FFFFFF" Background="#161620" BorderBrush="#2F2F42">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding ExecuteQueryCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
                <Button Grid.Column="1" Command="{Binding ExecuteQueryCommand}" 
                        Style="{StaticResource PrimaryButton}" Margin="12,0,0,0" Width="130" Height="42" Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <fa:ImageAwesome Icon="Play" Width="12" Foreground="Black" Margin="0,0,8,0"/>
                        <TextBlock Text="EXECUTE" FontSize="11" FontWeight="Black"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- Results Grid Area -->
        <Border Grid.Row="3" Style="{StaticResource StandardCard}" Padding="6">
            <Grid>
                <DataGrid ItemsSource="{Binding Results}" 
                          Style="{StaticResource NeuDataGrid}"
                          AutoGenerateColumns="True"
                          HeadersVisibility="Column"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Auto"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal">
                </DataGrid>

                <!-- Busy Overlay -->
                <Border Background="#CC0A0A0F" Visibility="{Binding IsBusy, Converter={StaticResource VisibilityConverter}}" CornerRadius="12">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <fa:ImageAwesome Icon="Spinner" Spin="True" Width="36" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,15"/>
                        <TextBlock Text="Querying WMI Repository..." Foreground="White" FontWeight="Bold" FontSize="12"/>
                    </StackPanel>
                </Border>

                <!-- Error Overlay -->
                <Border Background="#DD1A0A0F" Visibility="{Binding ErrorMessage, Converter={StaticResource VisibilityConverter}}" CornerRadius="12">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="25" MaxWidth="550">
                        <fa:ImageAwesome Icon="ExclamationTriangle" Width="36" Foreground="{DynamicResource CriticalBrush}" Margin="0,0,0,15"/>
                        <TextBlock Text="WMI Query Execution Error" Foreground="White" FontWeight="Black" FontSize="15" HorizontalAlignment="Center"/>
                        <Border Background="#2A1015" BorderBrush="{DynamicResource CriticalBrush}" BorderThickness="1" CornerRadius="6" Padding="12,8" Margin="0,12,0,0">
                            <TextBlock Text="{Binding ErrorMessage}" Foreground="#FFB3B3" TextWrapping="Wrap" FontSize="11" FontFamily="Consolas" HorizontalAlignment="Center" TextAlignment="Center"/>
                        </Border>
                        <Button Content="DISMISS ERROR" Command="{Binding DismissErrorCommand}" Style="{StaticResource StandardButton}" Margin="0,16,0,0" Width="140" Height="32" Cursor="Hand"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Footer Stats Bar -->
        <Grid Grid.Row="4" Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal">
                <fa:ImageAwesome Icon="ClockO" Width="10" Foreground="#7A7A88" Margin="0,0,6,0" VerticalAlignment="Center"/>
                <TextBlock Text="Execution Time: " Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11"/>
                <TextBlock Text="{Binding ExecutionTime}" Foreground="#FFFFFF" FontWeight="Bold" FontSize="11"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <fa:ImageAwesome Icon="Table" Width="10" Foreground="#7A7A88" Margin="0,0,6,0" VerticalAlignment="Center"/>
                <TextBlock Text="Rows Returned: " Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11"/>
                <TextBlock Text="{Binding Results.Rows.Count, FallbackValue=0}" Foreground="#FFFFFF" FontWeight="Bold" FontSize="11"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
