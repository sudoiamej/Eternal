<UserControl x:Class="Eternal.Views.Modules.RepairCenterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <Border Padding="40">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 1. Header -->
            <Grid Grid.Row="0" Style="{StaticResource ModuleHeaderStyle}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <TextBlock Text="ETERNAL" Style="{StaticResource Caption}" Foreground="{StaticResource AccentBrush}"/>
                        <TextBlock Text=" › " Style="{StaticResource Caption}"/>
                        <TextBlock Text="ETERNAL DOCTOR" Style="{StaticResource Caption}"/>
                    </StackPanel>
                    <TextBlock Text="Problem Solver" Style="{StaticResource H1}" Margin="0"/>
                </StackPanel>
            </Grid>

            <!-- 2. Intro -->
            <TextBlock Grid.Row="1" Text="What seems to be the problem today? Select an issue below for automated diagnosis and repair." 
                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,25" FontSize="13" FontStyle="Italic"/>

            <!-- 3. Problem List -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Problems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource StandardCard}" Margin="0,0,0,15" Padding="20">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Symptom Icon -->
                                    <Border Grid.Column="0" Width="48" Height="48" CornerRadius="8" Background="#10FFFFFF" Margin="0,0,20,0">
                                        <fa:ImageAwesome Icon="Stethoscope" Width="20" Foreground="{StaticResource InfoBrush}"/>
                                    </Border>

                                    <!-- Content -->
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="16" Foreground="White"/>
                                        <TextBlock Text="{Binding Symptom}" FontSize="12" Foreground="{StaticResource AccentBrush}" Margin="0,4,0,2" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Description}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                                    </StackPanel>

                                    <!-- Fix Button -->
                                    <Button Grid.Column="2" Command="{Binding FixCommand}" VerticalAlignment="Center" Width="120" Height="40" Style="{StaticResource PrimaryButton}">
                                        <StackPanel Orientation="Horizontal">
                                            <fa:ImageAwesome Icon="Wrench" Width="14" Margin="0,0,10,0" Foreground="White"/>
                                            <TextBlock Text="FIX NOW"/>
                                        </StackPanel>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- 4. Status Bar -->
            <Border Grid.Row="3" Background="#10FFFFFF" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="15" Margin="-40,0">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <fa:ImageAwesome Icon="CircleOutline" Spin="{Binding IsBusy}" Width="12" Foreground="{StaticResource InfoBrush}" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" FontSize="11"/>
                    </StackPanel>
                    <TextBlock HorizontalAlignment="Right" Text="V2.0.0 DIAGNOSTIC ENGINE" Foreground="{StaticResource TextSecondaryBrush}" Opacity="0.4" FontSize="9" VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
