<UserControl x:Class="Eternal.Views.Modules.PerformanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:vm="clr-namespace:Eternal.ViewModels.Modules"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Key="HistoryTitleStyle" BasedOn="{StaticResource Caption}">
            <Setter Property="Text" Value="CPU LOAD HISTORY (60 SECONDS)"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding SelectedHistory}" Value="{x:Static vm:HistoryType.Ram}">
                    <Setter Property="Text" Value="RAM USAGE HISTORY (60 SECONDS)"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding SelectedHistory}" Value="{x:Static vm:HistoryType.Disk}">
                    <Setter Property="Text" Value="DISK ACTIVE HISTORY (60 SECONDS)"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock" x:Key="HistoryDescriptionStyle">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="Text" Value="Real-time processor utilization trend"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding SelectedHistory}" Value="{x:Static vm:HistoryType.Ram}">
                    <Setter Property="Text" Value="Real-time physical memory consumption trend"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding SelectedHistory}" Value="{x:Static vm:HistoryType.Disk}">
                    <Setter Property="Text" Value="Real-time disk input/output activity trend"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Border Padding="40">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Header -->
                    <RowDefinition Height="Auto"/> <!-- Vitals Grid -->
                    <RowDefinition Height="*"/>    <!-- History/Trends -->
                </Grid.RowDefinitions>

                <!-- 1. Unified Header -->
                <Grid Grid.Row="0" Style="{StaticResource ModuleHeaderStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="ETERNAL" Style="{StaticResource Caption}" Foreground="{DynamicResource AccentBrush}"/>
                            <TextBlock Text=" › " Style="{StaticResource Caption}"/>
                            <TextBlock Text="LIVE TELEMETRY" Style="{StaticResource Caption}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Performance Monitor" Style="{StaticResource H1}" Margin="0"/>
                            <Ellipse Width="8" Height="8" Fill="{DynamicResource SuccessBrush}" Margin="15,0,0,12" VerticalAlignment="Center">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Style.Triggers>
                                            <EventTrigger RoutedEvent="Loaded">
                                                <BeginStoryboard>
                                                    <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1.0" To="0.2" Duration="0:0:1"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Grid.Column="1" VerticalAlignment="Bottom" Margin="0,0,0,5">
                        <Button Command="{Binding LoadCommand}" Style="{StaticResource IconButton}" ToolTip="Manual Recalibration">
                            <fa:ImageAwesome Icon="Refresh" Width="16" Foreground="{DynamicResource TextSecondaryBrush}"><fa:ImageAwesome.Effect><DropShadowEffect Color="{DynamicResource AccentColor}" BlurRadius="15" ShadowDepth="0" Opacity="0.3"/></fa:ImageAwesome.Effect></fa:ImageAwesome>
                        </Button>
                    </StackPanel>
                </Grid>

                <!-- 2. Core Vitals -->
                <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,30">
                    <Border Cursor="Hand">
                        <Border.Style>
                            <Style TargetType="Border" BasedOn="{StaticResource StandardCard}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedHistory}" Value="{x:Static vm:HistoryType.Cpu}">
                                        <Setter Property="Background" Value="#1A2B88D8"/>
                                        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding SelectHistoryCommand}" CommandParameter="{x:Static vm:HistoryType.Cpu}"/>
                        </Border.InputBindings>
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="CPU LOAD" Style="{StaticResource Caption}" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="{Binding CurrentCpu, StringFormat='{}{0:F0}%'}" FontSize="32" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                            <ProgressBar Value="{Binding CurrentCpu}" Height="3" Width="100" Margin="0,10,0,0" Background="#1AFFFFFF" Foreground="{DynamicResource AccentBrush}" BorderThickness="0"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Cursor="Hand">
                        <Border.Style>
                            <Style TargetType="Border" BasedOn="{StaticResource StandardCard}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedHistory}" Value="{x:Static vm:HistoryType.Ram}">
                                        <Setter Property="Background" Value="#1A007ACC"/>
                                        <Setter Property="BorderBrush" Value="{DynamicResource InfoBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding SelectHistoryCommand}" CommandParameter="{x:Static vm:HistoryType.Ram}"/>
                        </Border.InputBindings>
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="RAM USAGE" Style="{StaticResource Caption}" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="{Binding CurrentRam, StringFormat='{}{0:F0}%'}" FontSize="32" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                            <ProgressBar Value="{Binding CurrentRam}" Height="3" Width="100" Margin="0,10,0,0" Background="#1AFFFFFF" Foreground="{DynamicResource InfoBrush}" BorderThickness="0"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Cursor="Hand">
                        <Border.Style>
                            <Style TargetType="Border" BasedOn="{StaticResource StandardCard}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedHistory}" Value="{x:Static vm:HistoryType.Disk}">
                                        <Setter Property="Background" Value="#1AFFC107"/>
                                        <Setter Property="BorderBrush" Value="{DynamicResource WarningBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding SelectHistoryCommand}" CommandParameter="{x:Static vm:HistoryType.Disk}"/>
                        </Border.InputBindings>
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="DISK ACTIVE" Style="{StaticResource Caption}" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="{Binding CurrentDisk, StringFormat='{}{0:F0}%'}" FontSize="32" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                            <ProgressBar Value="{Binding CurrentDisk}" Height="3" Width="100" Margin="0,10,0,0" Background="#1AFFFFFF" Foreground="{DynamicResource WarningBrush}" BorderThickness="0"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <!-- 3. Trend Visualization -->
                <Border Grid.Row="2" Style="{StaticResource StandardCard}" Padding="30">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <StackPanel Grid.Row="0" Margin="0,0,0,20">
                            <TextBlock Style="{StaticResource HistoryTitleStyle}"/>
                            <TextBlock Style="{StaticResource HistoryDescriptionStyle}"/>
                        </StackPanel>

                        <ItemsControl Grid.Row="1" VerticalAlignment="Bottom" Height="150" ClipToBounds="True">
                            <ItemsControl.Style>
                                <Style TargetType="ItemsControl">
                                    <Setter Property="ItemsSource" Value="{Binding CpuHistory}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedHistory}" Value="{x:Static vm:HistoryType.Ram}">
                                            <Setter Property="ItemsSource" Value="{Binding RamHistory}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding SelectedHistory}" Value="{x:Static vm:HistoryType.Disk}">
                                            <Setter Property="ItemsSource" Value="{Binding DiskHistory}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ItemsControl.Style>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="60" Rows="1"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid VerticalAlignment="Bottom" Height="150">
                                        <Border Margin="0.5,0" Opacity="0.6" VerticalAlignment="Bottom">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                                                    <Setter Property="Height" Value="{Binding}"/>
                                                    <Setter Property="RenderTransform">
                                                        <Setter.Value>
                                                            <ScaleTransform ScaleY="1.5"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Setter Property="RenderTransformOrigin" Value="0.5,1"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding DataContext.SelectedHistory, RelativeSource={RelativeSource AncestorType=ItemsControl}}" Value="{x:Static vm:HistoryType.Ram}">
                                                            <Setter Property="Background" Value="{DynamicResource InfoBrush}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding DataContext.SelectedHistory, RelativeSource={RelativeSource AncestorType=ItemsControl}}" Value="{x:Static vm:HistoryType.Disk}">
                                                            <Setter Property="Background" Value="{DynamicResource WarningBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                        </Border>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <Grid Grid.Row="1" IsHitTestVisible="False" Opacity="0.1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" BorderBrush="{DynamicResource TextBrush}" BorderThickness="0,0,0,1"/>
                            <Border Grid.Row="1" BorderBrush="{DynamicResource TextBrush}" BorderThickness="0,0,0,1"/>
                            <Border Grid.Row="2" BorderBrush="{DynamicResource TextBrush}" BorderThickness="0,0,0,1"/>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </ScrollViewer>
    </Border>
</UserControl>




