<UserControl x:Class="Eternal.Views.Modules.BootView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <Border Padding="40">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 1. Header -->
            <Grid Grid.Row="0" Style="{StaticResource ModuleHeaderStyle}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <TextBlock Text="ETERNAL" Style="{StaticResource Caption}" Foreground="{DynamicResource AccentBrush}"/>
                        <TextBlock Text=" › " Style="{StaticResource Caption}"/>
                        <TextBlock Text="BOOT ARCHITECTURE" Style="{StaticResource Caption}"/>
                    </StackPanel>
                    <TextBlock Text="Boot Records" Style="{StaticResource H1}" Margin="0"/>
                </StackPanel>
                <Button Grid.Column="1" Command="{Binding LoadCommand}" Style="{StaticResource IconButton}" VerticalAlignment="Bottom">
                    <fa:ImageAwesome Icon="Refresh" Width="16" Foreground="{DynamicResource TextSecondaryBrush}" Spin="{Binding IsBusy}"><fa:ImageAwesome.Effect><DropShadowEffect Color="Transparent" BlurRadius="0" ShadowDepth="0" Opacity="0"/></fa:ImageAwesome.Effect></fa:ImageAwesome>
                </Button>
            </Grid>

            <!-- 2. Records List & BCD Sidebar -->
            <Grid Grid.Row="1" Margin="0,15,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="280"/>
                </Grid.ColumnDefinitions>

                <!-- Left Panel: Records List -->
                <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="0,0,20,0">
                    <ItemsControl ItemsSource="{Binding Records}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource StandardCard}" Margin="0,0,0,15">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Indicator Icon -->
                                        <Border Grid.Column="0" Width="40" Height="40" CornerRadius="20" Background="#10FFFFFF" Margin="0,0,20,0" VerticalAlignment="Top">
                                            <Grid>
                                                <fa:ImageAwesome Icon="ExternalLink" Width="16" Foreground="{DynamicResource InfoBrush}" Visibility="{Binding IsBootManager, Converter={StaticResource VisibilityConverter}}"><fa:ImageAwesome.Effect><DropShadowEffect Color="Transparent" BlurRadius="0" ShadowDepth="0" Opacity="0"/></fa:ImageAwesome.Effect></fa:ImageAwesome>
                                                <fa:ImageAwesome Icon="CheckCircle" Width="16" Foreground="{DynamicResource SuccessBrush}" Visibility="{Binding IsCurrent, Converter={StaticResource VisibilityConverter}}"><fa:ImageAwesome.Effect><DropShadowEffect Color="Transparent" BlurRadius="0" ShadowDepth="0" Opacity="0"/></fa:ImageAwesome.Effect></fa:ImageAwesome>
                                                <fa:ImageAwesome Icon="HddOutline" Width="16" Foreground="{DynamicResource TextSecondaryBrush}" 
                                                               Visibility="{Binding IsBootManager, Converter={StaticResource VisibilityConverter}, ConverterParameter=Inverse}"><fa:ImageAwesome.Effect><DropShadowEffect Color="Transparent" BlurRadius="0" ShadowDepth="0" Opacity="0"/></fa:ImageAwesome.Effect></fa:ImageAwesome>
                                            </Grid>
                                        </Border>

                                        <!-- Content -->
                                        <StackPanel Grid.Column="1">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Description}" FontWeight="Bold" FontSize="16" Foreground="White"/>
                                                <Border Background="#20FFFFFF" CornerRadius="2" Padding="4,1" Margin="10,0,0,0" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Identifier}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}" FontFamily="Consolas"/>
                                                </Border>
                                            </StackPanel>

                                            <UniformGrid Columns="2" Margin="0,15,0,0">
                                                <StackPanel Margin="0,0,10,10">
                                                    <TextBlock Text="DEVICE / PATH" Style="{StaticResource Caption}"/>
                                                    <TextBlock Text="{Binding Device}" FontSize="11" Foreground="White"/>
                                                    <TextBlock Text="{Binding Path}" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                                                </StackPanel>
                                                <StackPanel Margin="0,0,10,10">
                                                    <TextBlock Text="OS ARCHITECTURE" Style="{StaticResource Caption}"/>
                                                    <TextBlock Text="{Binding OsDevice}" FontSize="11" Foreground="White"/>
                                                    <TextBlock Text="{Binding SystemRoot}" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                                </StackPanel>
                                                <StackPanel Margin="0,0,10,0">
                                                    <TextBlock Text="RESUME OBJECT" Style="{StaticResource Caption}"/>
                                                    <TextBlock Text="{Binding ResumeObject}" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" FontFamily="Consolas"/>
                                                </StackPanel>
                                                <StackPanel Margin="0,0,10,0">
                                                    <TextBlock Text="BOOT POLICY" Style="{StaticResource Caption}"/>
                                                    <TextBlock Text="{Binding BootMenuPolicy}" FontSize="11" Foreground="White"/>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="{Binding Nx, StringFormat='NX: {0}'}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                                        <TextBlock Text=" | SafeBoot: Active" FontSize="10" Foreground="{DynamicResource SuccessBrush}" Visibility="{Binding IsSafeBoot, Converter={StaticResource VisibilityConverter}}" Margin="5,0,0,0"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </UniformGrid>
                                        </StackPanel>

                                        <!-- Card Actions -->
                                        <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="20,0,0,0" Width="100">
                                            <Button Command="{Binding DataContext.ToggleSafeBootCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource StandardButton}" 
                                                    Padding="5,4" Margin="0,0,0,8" Height="26">
                                                <StackPanel Orientation="Horizontal">
                                                    <fa:ImageAwesome Icon="Shield" Width="10" Margin="0,0,5,0">
                                                        <fa:ImageAwesome.Style>
                                                            <Style TargetType="fa:ImageAwesome">
                                                                <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsSafeBoot}" Value="True">
                                                                        <Setter Property="Foreground" Value="{DynamicResource SuccessBrush}"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </fa:ImageAwesome.Style>
                                                    </fa:ImageAwesome>
                                                    <TextBlock Text="Safe Boot" FontSize="10" VerticalAlignment="Center" Foreground="White"/>
                                                </StackPanel>
                                            </Button>

                                            <Button Command="{Binding DataContext.DeleteEntryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                    CommandParameter="{Binding}"
                                                    Foreground="{DynamicResource DangerBrush}"
                                                    Padding="5,4" Height="26">
                                                <Button.Style>
                                                    <Style TargetType="Button" BasedOn="{StaticResource StandardButton}">
                                                        <Setter Property="IsEnabled" Value="True"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsBootManager}" Value="True">
                                                                <Setter Property="IsEnabled" Value="False"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                                                <Setter Property="IsEnabled" Value="False"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                                <StackPanel Orientation="Horizontal">
                                                    <fa:ImageAwesome Icon="Trash" Width="10" Margin="0,0,5,0" Foreground="{DynamicResource DangerBrush}"/>
                                                    <TextBlock Text="Delete" FontSize="10" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Right Panel: BCD Editor Settings Sidebar -->
                <Border Grid.Column="1" Style="{StaticResource StandardCard}" VerticalAlignment="Top">
                    <StackPanel Margin="15">
                        <TextBlock Text="BCD PARAMETERS" FontWeight="Bold" FontSize="12" Foreground="White" Margin="0,0,0,15"/>

                        <TextBlock Text="BOOT MENU TIMEOUT" Style="{StaticResource Caption}" Margin="0,0,0,5"/>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Slider Value="{Binding BootTimeout, Mode=TwoWay}" Minimum="0" Maximum="120" TickFrequency="5" IsSnapToTickEnabled="True" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="{Binding BootTimeout, StringFormat='{}{0}s'}" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="10,0,0,0" Width="30" TextAlignment="Right"/>
                        </Grid>

                        <Button Command="{Binding SaveTimeoutCommand}" Style="{StaticResource StandardButton}" Content="Save Timeout" Margin="0,0,0,20" Height="30"/>

                        <Separator Background="#20FFFFFF" Margin="0,0,0,15"/>

                        <TextBlock Text="SYSTEM HARDENING" FontWeight="Bold" FontSize="12" Foreground="White" Margin="0,0,0,10"/>
                        <TextBlock Text="Select a record on the left to configure advanced boot structures. Safe Boot toggles allow diagnostic isolation. Delete command is constrained to guard critical loaders." TextWrapping="Wrap" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" LineHeight="15"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</UserControl>




