<UserControl x:Class="Eternal.Views.Modules.CategoryGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1100">

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,50">
            <TextBlock Text="CATEGORY" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="Black" Opacity="0.7"/>
            <TextBlock Text="{Binding Title}" Foreground="{DynamicResource TextBrush}" FontSize="36" FontWeight="Black"/>
        </StackPanel>

        <Viewbox Grid.Row="1" Stretch="Uniform" VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="1100" MaxHeight="600">
            <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="1000"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource CarPlayAppTile}" 
                                Width="180" Height="140" 
                                Margin="10"
                                Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                CommandParameter="{Binding ViewName}"
                                PreviewMouseLeftButtonDown="Tile_PreviewMouseLeftButtonDown"
                                PreviewMouseMove="Tile_PreviewMouseMove">
                            <Button.RenderTransform>
                                <ScaleTransform CenterX="90" CenterY="70"/>
                            </Button.RenderTransform>
                            <Button.Triggers>
                                <EventTrigger RoutedEvent="Button.Loaded">
                                    <BeginStoryboard Storyboard="{StaticResource TileEntranceAnim}"/>
                                </EventTrigger>
                            </Button.Triggers>
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <!-- Translucent premium circular plate for the sub-view icon -->
                                <Border Width="46" Height="46" CornerRadius="23" Margin="0,0,0,10" Background="#16FFFFFF" BorderBrush="#0AFFFFFF" BorderThickness="1" HorizontalAlignment="Center">
                                    <fa:ImageAwesome Icon="{Binding Icon}" Width="18" Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="{Binding Name}" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" Width="150"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Viewbox>
    </Grid>
</UserControl>
