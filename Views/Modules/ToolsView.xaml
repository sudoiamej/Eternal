<UserControl x:Class="Eternal.Views.Modules.ToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" d:DesignHeight="550" d:DesignWidth="800"
             SnapsToDevicePixels="True"
             UseLayoutRounding="True">
    
    <Border Padding="30">
        <Grid>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Header -->
                    <Grid Style="{StaticResource ModuleHeaderStyle}" Margin="0,0,0,25">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                <TextBlock Text="ETERNAL" Style="{StaticResource Caption}" Foreground="{DynamicResource AccentBrush}"/>
                                <TextBlock Text=" › " Style="{StaticResource Caption}"/>
                                <TextBlock Text="SYSTEM TOOLKIT" Style="{StaticResource Caption}"/>
                            </StackPanel>
                            <TextBlock Text="Repair &amp; Maintenance" Style="{StaticResource H1}" Margin="0"/>
                        </StackPanel>
                    </Grid>

                    <!-- Section 1: Maintenance Actions -->
                    <TextBlock Text="MAINTENANCE ACTIONS" Style="{StaticResource Caption}" Margin="5,0,0,10"/>
                    <UniformGrid Columns="2" Margin="0,0,0,20">
                        <Button Command="{Binding FlushDnsCommand}" Style="{StaticResource CardButton}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <fa:ImageAwesome Icon="Globe" Width="16" Margin="0,0,15,0" Foreground="{DynamicResource AccentBrush}"/>
                                <StackPanel>
                                    <TextBlock Text="Flush DNS Cache" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                                    <TextBlock Text="Clears system DNS resolver records." FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                </StackPanel>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding ClearTempCommand}" Style="{StaticResource CardButton}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <fa:ImageAwesome Icon="TrashOutline" Width="16" Margin="0,0,15,0" Foreground="{DynamicResource AccentBrush}"/>
                                <StackPanel>
                                    <TextBlock Text="Clear Temp Files" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                                    <TextBlock Text="Deletes files in the system temp directory." FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                </StackPanel>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding RebuildIconsCommand}" Style="{StaticResource CardButton}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <fa:ImageAwesome Icon="Refresh" Width="16" Margin="0,0,15,0" Foreground="{DynamicResource AccentBrush}"/>
                                <StackPanel>
                                    <TextBlock Text="Rebuild Icon Cache" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                                    <TextBlock Text="Forces Windows to reload explorer icons." FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                </StackPanel>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding ClearEventLogsCommand}" Style="{StaticResource CardButton}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <fa:ImageAwesome Icon="List" Width="16" Margin="0,0,15,0" Foreground="{DynamicResource WarningBrush}"/>
                                <StackPanel>
                                    <TextBlock Text="Clear Event Logs" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                                    <TextBlock Text="Purge all Windows event logs." FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                </StackPanel>
                            </StackPanel>
                        </Button>
                    </UniformGrid>

                    <!-- Section 2: Integrity Repair -->
                    <TextBlock Text="SYSTEM INTEGRITY REPAIR" Style="{StaticResource Caption}" Margin="5,0,0,10"/>
                    <UniformGrid Columns="2" Margin="0,0,0,20">
                        <Button Command="{Binding RepairSystemFilesCommand}" Style="{StaticResource CardButton}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <fa:ImageAwesome Icon="Shield" Width="16" Margin="0,0,15,0" Foreground="{DynamicResource InfoBrush}"/>
                                <StackPanel>
                                    <TextBlock Text="SFC Scan &amp; Repair" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                                    <TextBlock Text="Repairs corrupted system files (Admin)." FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                </StackPanel>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding RunDismCommand}" Style="{StaticResource CardButton}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <fa:ImageAwesome Icon="Magic" Width="16" Margin="0,0,15,0" Foreground="{DynamicResource InfoBrush}"/>
                                <StackPanel>
                                    <TextBlock Text="DISM Health Restore" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                                    <TextBlock Text="Repairs Windows Component Store (Admin)." FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                </StackPanel>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding ResetWindowsUpdateCommand}" Style="{StaticResource CardButton}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <fa:ImageAwesome Icon="Download" Width="16" Margin="0,0,15,0" Foreground="{DynamicResource InfoBrush}"/>
                                <StackPanel>
                                    <TextBlock Text="Reset Windows Update" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                                    <TextBlock Text="Fixes stuck or broken update services." FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                </StackPanel>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding ResetNetworkCommand}" Style="{StaticResource CardButton}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <fa:ImageAwesome Icon="Wifi" Width="16" Margin="0,0,15,0" Foreground="{DynamicResource CriticalBrush}"/>
                                <StackPanel>
                                    <TextBlock Text="Reset Network Stack" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                                    <TextBlock Text="Reinitializes Winsock and IP stack." FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                </StackPanel>
                            </StackPanel>
                        </Button>
                    </UniformGrid>

                    <!-- Section 3: Performance -->
                    <TextBlock Text="PERFORMANCE OPTIMIZATION" Style="{StaticResource Caption}" Margin="5,0,0,10"/>
                    <UniformGrid Columns="2">
                        <Button Command="{Binding OptimizeBootCommand}" Style="{StaticResource CardButton}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <fa:ImageAwesome Icon="Bolt" Width="16" Margin="0,0,15,0" Foreground="{DynamicResource SuccessBrush}"/>
                                <StackPanel>
                                    <TextBlock Text="Optimize Boot Performance" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                                    <TextBlock Text="Realigns boot files for faster startup." FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                </StackPanel>
                            </StackPanel>
                        </Button>
                    </UniformGrid>
                    
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
