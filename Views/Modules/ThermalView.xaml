<UserControl x:Class="Eternal.Views.Modules.ThermalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <Border Padding="40">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Header -->
                    <RowDefinition Height="Auto"/> <!-- Vitals -->
                    <RowDefinition Height="*"/>    <!-- Details -->
                </Grid.RowDefinitions>

                <!-- 1. Standardized Header (HCI Rule 9: Consistency) -->
                <Grid Grid.Row="0" Style="{StaticResource ModuleHeaderStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="ETERNAL" Style="{StaticResource Caption}" Foreground="{StaticResource AccentBrush}"/>
                            <TextBlock Text=" › " Style="{StaticResource Caption}"/>
                            <TextBlock Text="THERMAL &amp; POWER" Style="{StaticResource Caption}"/>
                        </StackPanel>
                        <TextBlock Text="Thermal Intelligence" Style="{StaticResource H1}" Margin="0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" VerticalAlignment="Bottom" Margin="0,0,0,5">
                        <Button Command="{Binding LoadCommand}" IsEnabled="{Binding IsLoading, Converter={StaticResource VisibilityConverter}, ConverterParameter=Inverse}" Style="{StaticResource IconButton}" ToolTip="Rescan Sensors">
                            <fa:ImageAwesome Icon="Refresh" Width="16" Foreground="{StaticResource TextSecondaryBrush}"/>
                        </Button>
                    </StackPanel>
                </Grid>

                <!-- 2. Primary Telemetry (HCI Rule 7: System Status) -->
                <UniformGrid Grid.Row="1" Columns="2" Margin="0,0,0,25">
                    <!-- Thermal Gauge Card -->
                    <Border Style="{StaticResource StandardCard}" Margin="0,0,12,0" Padding="30">
                        <StackPanel>
                            <TextBlock Text="CORE TEMPERATURE" Style="{StaticResource Caption}" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                            <Grid HorizontalAlignment="Center">
                                <Ellipse Width="120" Height="120" Stroke="{StaticResource BorderBrush}" StrokeThickness="8"/>
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding CpuTempText}" FontSize="28" FontWeight="Black" HorizontalAlignment="Center"/>
                                    <Border Background="{StaticResource SuccessBrush}" Padding="8,2" CornerRadius="2" Margin="0,5,0,0" HorizontalAlignment="Center">
                                        <TextBlock Text="NOMINAL" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                    </Border>
                                </StackPanel>
                            </Grid>
                            <TextBlock Text="Thermal zones update via ACPI telemetry" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,15,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Power State Card (HCI Rule 5: Mapping) -->
                    <Border Style="{StaticResource StandardCard}" Margin="12,0,0,0" Padding="30">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="POWER ARCHITECTURE" Style="{StaticResource Caption}" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                            
                            <!-- AC Logic -->
                            <StackPanel Visibility="{Binding Data.HasBattery, Converter={StaticResource VisibilityConverter}, ConverterParameter=Inverse}">
                                <fa:ImageAwesome Icon="Plug" Width="40" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                                <TextBlock Text="DIRECT AC POWER" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="Desktop configuration detected" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                            </StackPanel>

                            <!-- Battery Logic -->
                            <StackPanel Visibility="{Binding Data.HasBattery, Converter={StaticResource VisibilityConverter}}">
                                <Grid Margin="0,0,0,10" HorizontalAlignment="Center">
                                    <fa:ImageAwesome Icon="BatteryFull" Width="48" Foreground="{StaticResource SuccessBrush}"/>
                                    <TextBlock Text="{Binding Data.BatteryPercent}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="12" FontWeight="Bold" Margin="0,0,5,0"/>
                                </Grid>
                                <TextBlock Text="{Binding Data.PowerSource}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Data.BatteryStatus}" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <!-- 3. HCI Intelligence Card (HCI Rule 6: Conceptual Model) -->
                <Border Grid.Row="2" Style="{StaticResource StandardCard}" Padding="25" BorderBrush="{StaticResource InfoBrush}" BorderThickness="0,0,0,4" VerticalAlignment="Top">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <fa:ImageAwesome Icon="LightbulbOutline" Width="24" Foreground="{StaticResource InfoBrush}" VerticalAlignment="Top" Margin="0,2,20,0"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="ENVIRONMENTAL ANALYSIS" Style="{StaticResource Caption}" Margin="0,0,0,10"/>
                            <TextBlock Text="{Binding ConclusionText}" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" TextWrapping="Wrap" LineHeight="22"/>
                            <TextBlock Text="Based on active power profile and thermal headroom." FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Loading Overlay (HCI Rule 7: Status) -->
                <Border Grid.RowSpan="3" Background="#CC0A0A0A" Visibility="{Binding IsLoading, Converter={StaticResource VisibilityConverter}}" Margin="-40">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <fa:ImageAwesome Icon="Spinner" Spin="True" Foreground="{StaticResource AccentBrush}" Width="48" Margin="0,0,0,20"/>
                        <TextBlock Text="Polling Thermal Sensors..." FontSize="18" Foreground="{StaticResource TextBrush}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>
    </Border>
</UserControl>