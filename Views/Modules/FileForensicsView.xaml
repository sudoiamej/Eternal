<UserControl x:Class="Eternal.Views.Modules.FileForensicsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="1000">
    <Border Padding="40">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Style="{StaticResource ModuleHeaderStyle}">
                <TextBlock Text="ETERNAL › SECURITY FORENSICS" Style="{StaticResource HeaderBreadcrumb}"/>
                <TextBlock Text="File Integrity Forensics" Style="{StaticResource H1}" Margin="0"/>
            </StackPanel>

            <DockPanel Grid.Row="1" Margin="0,20,0,0">
                <!-- Right: Analysis Result (Fixed Width, docked right) -->
                <StackPanel DockPanel.Dock="Right" Width="450" Margin="20,0,0,0">
                    <TextBlock Text="ANALYSIS BRIEF" Style="{StaticResource Caption}" Margin="0,0,0,15"/>
                    
                    <Border Visibility="{Binding HasResult, Converter={StaticResource VisibilityConverter}}" 
                            Style="{StaticResource StandardCard}" Padding="25">
                        <StackPanel>
                            <TextBlock Text="{Binding AnalysisResult.FileName, FallbackValue='No File'}" FontSize="18" FontWeight="Bold" Foreground="White" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding AnalysisResult.FilePath, FallbackValue='N/A'}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}" TextTrimming="PathEllipsis" Margin="0,5,0,20"/>

                            <!-- Signature Status -->
                            <Border Padding="15" CornerRadius="8" Margin="0,0,0,20">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1AFFFFFF"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding AnalysisResult.IsTrusted}" Value="True">
                                                <Setter Property="Background" Value="#1A4CAF50"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding AnalysisResult.SignatureStatus}" Value="Unsigned">
                                                <Setter Property="Background" Value="#1AF44336"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <StackPanel>
                                        <TextBlock Text="SIGNATURE STATUS" Style="{StaticResource Caption}" Margin="0,0,0,5"/>
                                        <TextBlock Text="{Binding AnalysisResult.SignatureStatus, FallbackValue='Unknown'}" FontWeight="Black" FontSize="14" Foreground="White"/>
                                        <TextBlock Text="{Binding AnalysisResult.SignerName, FallbackValue='N/A'}" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,5,0,0" TextWrapping="Wrap"/>
                                    </StackPanel>
                                    
                                    <fa:ImageAwesome Icon="Shield" Foreground="{DynamicResource WarningBrush}" Width="24" HorizontalAlignment="Right" VerticalAlignment="Center">
                                        <fa:ImageAwesome.Style>
                                            <Style TargetType="fa:ImageAwesome">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding AnalysisResult.IsTrusted}" Value="True">
                                                        <Setter Property="Icon" Value="CheckCircle"/>
                                                        <Setter Property="Foreground" Value="{DynamicResource SuccessBrush}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding AnalysisResult.SignatureStatus}" Value="Unsigned">
                                                        <Setter Property="Icon" Value="ExclamationTriangle"/>
                                                        <Setter Property="Foreground" Value="{DynamicResource CriticalBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </fa:ImageAwesome.Style>
                                    </fa:ImageAwesome>
                                </Grid>
                            </Border>

                            <!-- Hashes -->
                            <TextBlock Text="CRYPTOGRAPHIC HASHES" Style="{StaticResource Caption}" Margin="0,0,0,10"/>
                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="SHA256" FontSize="9" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding AnalysisResult.Sha256Hash, Mode=OneWay, FallbackValue=''}" IsReadOnly="True" Background="#10FFFFFF" BorderThickness="0" Foreground="White" FontSize="10" Padding="5" Margin="0,5,0,10"/>
                                
                                <TextBlock Text="MD5" FontSize="9" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <TextBox Text="{Binding AnalysisResult.Md5Hash, Mode=OneWay, FallbackValue=''}" IsReadOnly="True" Background="#10FFFFFF" BorderThickness="0" Foreground="White" FontSize="10" Padding="5"/>
                            </StackPanel>

                            <Button Command="{Binding ClearCommand}" Content="CLEAR ANALYSIS" Style="{StaticResource StandardButton}" HorizontalAlignment="Stretch"/>
                        </StackPanel>
                    </Border>

                    <!-- Placeholder -->
                    <Border Visibility="{Binding HasResult, Converter={StaticResource VisibilityConverter}, ConverterParameter=Inverse}" 
                            Style="{StaticResource StandardCard}" Padding="40" Opacity="0.5">
                        <TextBlock Text="Analyze a file to see its cryptographic signature and integrity brief." 
                                   TextWrapping="Wrap" TextAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </Border>
                </StackPanel>

                <!-- Left: Drop Zone (Fill) -->
                <Border BorderBrush="{DynamicResource BorderBrush}" 
                        BorderThickness="2" CornerRadius="15" Background="#0AFFFFFF" 
                        AllowDrop="True" Drop="FileZone_Drop" DragOver="FileZone_DragOver">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <fa:ImageAwesome Icon="FileTextOutline" Width="64" Foreground="{DynamicResource AccentBrush}" Opacity="0.5" Margin="0,0,0,25"/>
                        <TextBlock Text="DRAG &amp; DROP FILE TO ANALYZE" FontSize="20" FontWeight="Black" Foreground="White" HorizontalAlignment="Center"/>
                        <TextBlock Text="Verifying MD5, SHA256 and Authenticode Signatures" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>
            </DockPanel>
        </Grid>
    </Border>
</UserControl>
