<UserControl x:Class="Eternal.Views.Modules.PortScannerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#0A0A10" BorderBrush="#1C1C28" BorderThickness="0,0,0,1" Padding="16,12">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <fa:ImageAwesome Icon="Shield" Width="16" Foreground="{DynamicResource AccentBrush}" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <StackPanel>
                        <TextBlock Text="ETERNAL STEALTH NETWORK PORT SCANNER" FontSize="14" FontWeight="Black" Foreground="White"/>
                        <TextBlock Text="Audits open network ports &amp; listening service threat vectors" FontSize="10" Foreground="#888896"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Controls Bar -->
        <Border Grid.Row="1" Background="#060609" BorderBrush="#161622" BorderThickness="0,0,0,1" Padding="16,10">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <TextBlock Text="Target IP / Host:" FontSize="11" FontWeight="Bold" Foreground="#888896" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Border Background="#12121C" BorderBrush="#262638" BorderThickness="1" CornerRadius="6" Padding="10,0" Height="32" Width="200" Margin="0,0,12,0">
                        <TextBox Text="{Binding TargetHost, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" Foreground="#00E5FF" FontFamily="Consolas" FontSize="12" FontWeight="Bold" VerticalAlignment="Center"/>
                    </Border>
                    <Button Command="{Binding StartPortScanCommand}" IsEnabled="{Binding IsScanning, Converter={StaticResource InvertBooleanConverter}}" Style="{StaticResource PrimaryButton}" Height="32" Padding="16,0">
                        <StackPanel Orientation="Horizontal">
                            <fa:ImageAwesome Icon="Search" Width="11" Foreground="Black" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="LAUNCH PORT SCAN" FontSize="11" FontWeight="Black" Foreground="Black"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
                <TextBlock Text="{Binding OpenPortsSummary}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- ListView Open Ports Grid -->
        <ListView Grid.Row="2" ItemsSource="{Binding ScanResults}" Background="#030305" BorderThickness="0" Foreground="White">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Margin" Value="6,2"/>
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border x:Name="ItemBorder" Background="{TemplateBinding Background}" BorderBrush="Transparent" BorderThickness="1" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                    <GridViewRowPresenter Columns="{TemplateBinding GridView.ColumnCollection}" Content="{TemplateBinding Content}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ItemBorder" Property="Background" Value="#0F1B2A"/>
                                        <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#00E5FF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Port" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Port, StringFormat='Port {0}'}" FontWeight="Bold" FontFamily="Consolas" Foreground="#00E5FF"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Service Name" Width="240">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ServiceName}" FontWeight="Bold" Foreground="White"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="State" Width="140">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="#0D0D14" BorderBrush="{Binding StateColor}" BorderThickness="1" CornerRadius="4" Padding="6,2">
                                    <TextBlock Text="{Binding State}" FontSize="10" FontWeight="Bold" Foreground="{Binding StateColor}"/>
                                </Border>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Threat Risk Level" Width="180">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding RiskLevel}" FontWeight="Bold" Foreground="{Binding StateColor}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Status Bar with Progress -->
        <Border Grid.Row="3" Background="#0A0A10" BorderBrush="#1C1C28" BorderThickness="0,1,0,0" Padding="16,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding StatusText}" FontSize="10" Foreground="#888896" VerticalAlignment="Center"/>
                <ProgressBar Grid.Column="1" Value="{Binding ScanProgress}" Minimum="0" Maximum="100" Height="8" Background="#12121D" Foreground="{DynamicResource AccentBrush}" BorderThickness="0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
