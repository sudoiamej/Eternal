<UserControl x:Class="Eternal.Views.Modules.TuningView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <Border Padding="40">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 1. Header -->
            <Grid Grid.Row="0" Style="{StaticResource ModuleHeaderStyle}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <TextBlock Text="ETERNAL" Style="{StaticResource Caption}" Foreground="{StaticResource AccentBrush}"/>
                        <TextBlock Text=" › " Style="{StaticResource Caption}"/>
                        <TextBlock Text="SYSTEM OPTIMIZATION" Style="{StaticResource Caption}"/>
                    </StackPanel>
                    <TextBlock Text="Guardian Tuning" Style="{StaticResource H1}" Margin="0"/>
                </StackPanel>
                <Button Grid.Column="1" Command="{Binding CreateRestorePointCommand}" Style="{StaticResource SecondaryButton}" VerticalAlignment="Bottom">
                    <StackPanel Orientation="Horizontal">
                        <fa:ImageAwesome Icon="Shield" Width="12" Margin="0,0,8,0" Foreground="White"/>
                        <TextBlock Text="CREATE RESTORE POINT"/>
                    </StackPanel>
                </Button>
            </Grid>

            <!-- 2. Safety Notice -->
            <Border Grid.Row="1" Background="#15FFD700" BorderBrush="#30FFD700" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal">
                    <fa:ImageAwesome Icon="ExclamationTriangle" Width="16" Foreground="#FFFFD700" Margin="0,0,15,0"/>
                    <TextBlock Text="SAFETY FIRST: Registry tweaks can affect system stability. Always create a Restore Point before applying changes." 
                               VerticalAlignment="Center" Foreground="#CCFFFFFF" FontSize="12"/>
                </StackPanel>
            </Border>

            <!-- 3. Tweak List -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Tweaks}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource StandardCard}" Margin="0,0,0,10" Padding="20">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Category Icon -->
                                    <Border Grid.Column="0" Width="40" Height="40" CornerRadius="20" Background="#10FFFFFF" Margin="0,0,20,0">
                                        <fa:ImageAwesome Icon="Cogs" Width="18" Foreground="{StaticResource TextSecondaryBrush}"/>
                                    </Border>

                                    <!-- Details -->
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Foreground="White"/>
                                        <TextBlock Text="{Binding Description}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,2,0,0"/>
                                    </StackPanel>

                                    <!-- Toggle -->
                                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                        <Button Command="{Binding DataContext.ToggleTweakCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                                CommandParameter="{Binding}"
                                                Width="100" Height="32">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
                                                    <Setter Property="Content" Value="APPLY"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsApplied}" Value="True">
                                                            <Setter Property="Content" Value="REVERT"/>
                                                            <Setter Property="Background" Value="#30FFFFFF"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
