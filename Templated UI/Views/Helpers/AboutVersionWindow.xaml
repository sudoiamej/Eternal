<Window x:Class="Eternal.Views.Helpers.AboutVersionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        mc:Ignorable="d"
        Title="About Version" Height="500" Width="850"
        WindowStartupLocation="CenterOwner"
        AllowsTransparency="True" WindowStyle="None" Background="Transparent">
    <Border Background="{DynamicResource SidebarBrush}" CornerRadius="24" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="340"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Close Button (Standardized Placement) -->
            <Button VerticalAlignment="Top" HorizontalAlignment="Right" Grid.Column="1" 
                    Margin="0,25,25,0" Style="{StaticResource IconButton}" Click="Close_Click" Panel.ZIndex="10">
                <fa:ImageAwesome Icon="Close" Width="14" Foreground="{DynamicResource TextDisabledBrush}"/>
            </Button>

            <!-- Left: Branding (HCI: Focused Identity) -->
            <Border Grid.Column="0" Background="{DynamicResource BackgroundBrush}" CornerRadius="24,0,0,24" Padding="40">
                <StackPanel VerticalAlignment="Center">
                    <Image Source="/Assets/logo.png" Width="110" Margin="0,0,0,35" HorizontalAlignment="Center">
                        <Image.Effect>
                            <DropShadowEffect Color="{DynamicResource AccentColor}" BlurRadius="40" ShadowDepth="0" Opacity="0.5"/>
                        </Image.Effect>
                    </Image>
                    
                    <TextBlock Text="ETERNAL" FontSize="32" FontWeight="Black" Foreground="White" HorizontalAlignment="Center"/>
                    <TextBlock Text="SYSTEM INTELLIGENCE" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" Margin="0,-2,0,40" Opacity="0.9"/>

                    <Border Background="#12FFFFFF" Padding="25,20" CornerRadius="16" BorderBrush="#1AFFFFFF" BorderThickness="1">
                        <StackPanel>
                            <Grid Margin="0,0,0,14">
                                <TextBlock Text="Version" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
                                <TextBlock Text="3.0.0" Foreground="White" FontWeight="Bold" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,14">
                                <TextBlock Text="Milestone" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
                                <TextBlock Text="Gold Release" Foreground="White" FontWeight="Bold" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,14">
                                <TextBlock Text="Branch" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
                                <TextBlock Text="Main" Foreground="{DynamicResource SuccessBrush}" FontWeight="Bold" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid>
                                <TextBlock Text="Build Date" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
                                <TextBlock x:Name="BuildDateText" Text="2026-06-03" Foreground="White" FontWeight="Bold" HorizontalAlignment="Right"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <!-- Right: Credits (HCI: Clean Alignment & Scannability) -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="60,40">
                <TextBlock Text="ENGINEERING CREDITS" Style="{StaticResource Caption}" Margin="0,0,0,35"/>

                <!-- Unified Grid for all Credits -->
                <Grid Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Row 1: Human Team -->
                    <StackPanel Grid.Row="0" Grid.Column="0">
                        <TextBlock Text="Lead Developer" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                        <TextBlock Text="Ethan Jefferson A." FontSize="18" FontWeight="SemiBold" Foreground="White"/>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1">
                        <TextBlock Text="UI Review &amp; QA" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                        <TextBlock Text="Bernadette De Luna" FontSize="18" FontWeight="SemiBold" Foreground="White"/>
                    </StackPanel>

                    <!-- Row 2: AI Support -->
                    <StackPanel Grid.Row="2" Grid.ColumnSpan="2">
                        <TextBlock Text="Autonomous Intelligence" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                        <StackPanel Orientation="Horizontal">
                            <fa:ImageAwesome Icon="Google" Width="16" Foreground="{DynamicResource InfoBrush}" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Google Gemini CLI" FontSize="18" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>

                <!-- Dedication Card (Standardized with App Styles) -->
                <Border Style="{StaticResource StandardCard}" Background="#0AFFFFFF" Padding="25" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1,0,0,0">
                    <StackPanel>
                        <fa:ImageAwesome Icon="Heart" Width="18" Foreground="{DynamicResource CriticalBrush}" HorizontalAlignment="Left" Margin="0,0,0,15">
                            <fa:ImageAwesome.Effect>
                                <DropShadowEffect Color="{DynamicResource CriticalColor}" BlurRadius="15" Opacity="0.4"/>
                            </fa:ImageAwesome.Effect>
                        </fa:ImageAwesome>
                        <TextBlock Text="To my one and only, the love of my life who helped me succeed this application. Thank you, Bernadette De Luna &lt;3" 
                                   Foreground="White" FontSize="14" TextWrapping="Wrap" LineHeight="22" FontStyle="Italic" Opacity="0.9"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Border>
</Window>
